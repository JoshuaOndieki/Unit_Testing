{
  "name": "basic-testing",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "tsc",
    "test": "concurrently --kill-others \"json-server testdb.json --watch\" \"vitest --run --reporter verbose\"",
    "test:watch": "vitest",
    "start": "node app"
  },
  "keywords": [],
  "author": "Joshua Ondieki",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.0.1",
    "vitest": "^0.30.1"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
